import SwiftUI
enum EFSpacing { static let page: CGFloat = 20; static let section: CGFloat = 16; static let grid: CGFloat = 12 }
enum EFRadius  { static let card: CGFloat = 16 }
enum EFSafe    { static let bottom: CGFloat = 24 }